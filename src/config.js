/***
 * Configs
 */

import { site } from 'json!yaml!../data/site.yml'
import { speaker } from 'json!yaml!../data/speaker.yml'
import { sponsor } from 'json!yaml!../data/sponsor.yml'

export default {
    site,
    speaker,
    sponsor
}
